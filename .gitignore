# Собирается автоматически из curated.json + feeds.json
public/

# Фото товаров с CDN магазина — рабочие копии для примерки.
# НЕ публикуем: перехостить чужие картинки нельзя (копирайт).
garments/

# Результаты примерки до публикации (в сеть уходят через products/)
tryon_out/

# Локальные секреты/настройки и мусор
.chrome_profile/
feed_url.txt
__pycache__/
*.pyc

# Фото товаров, скачанные для разметки пола по снимку (classify_by_photo.py).
# НЕ публикуем по той же причине, что garments/ — это картинки магазина.
photo_cache/

# Ноутбуки примерки: внутрь вшиты base64 товарных фото
vton/*.ipynb

# Резервные копии, которые делают скрипты чистки
*.bak
*.bak2

# Рабочие вырезки. В публикацию идут копии в products/ под именем cut_*,
# держать оба набора в истории репозитория незачем.
products_cut/

# ????????? ????-????????: ??????? ?????, ? ??????????? ?? ?????
base_candidates/

# ??????????? ????? ?????: ????? ??????, ? ??????????? ?? ?????????
shoes_raw/
